# Security Policy

## 报告安全漏洞

如果你发现安全漏洞，请**不要**在公开 Issue 中报告。请通过以下方式联系：

1. 在 GitHub 上创建一个 **私密 Issue**
2. 或发送邮件至项目维护者

我们会在收到报告后 48 小时内回复，并在修复后公开致谢。

## 安全设计

### 凭据存储

- 云服务凭据存储在本地 `~/.pi/agent/config-backup.json`
- 凭据**不会**被备份到云端
- 凭据**不会**随配置同步

### 路径安全

- 文件恢复时会验证路径，防止路径穿越攻击
- 所有文件操作限定在 `~/.pi/agent/` 目录内

### 依赖安全

- 定期更新依赖版本
- 使用 `npm audit` 检查已知漏洞

## 已知安全考虑

- 本插件运行在本地环境中，具有完整的文件系统访问权限
- 云服务凭据由用户自行管理，请确保使用强密码
- GitHub Gist Token 建议使用最小权限范围（仅 `gist` scope）

## 版本支持

我们仅支持最新版本的安全更新。请始终使用最新版本。
